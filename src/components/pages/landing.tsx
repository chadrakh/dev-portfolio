import { useEffect } from 'react';

const Landing : React.FC = () => {
    useEffect(() => {
        document.title = 'Home';
      }, []);
      
    return (
        <>
        
        </>
    );
}

export default Landing;